  <div class="content is-medium">
    <h2 class="title is-2">My Products List</h2>
    <ul>
      <% @products.each do | product | %>
        <li>
          <%= link_to product.title, product %> - <%= product.category.name %>
        </li>
      <% end %>
    </ul>
  </div>
